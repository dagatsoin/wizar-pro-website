import { Styles } from '@sproutch/ui'

export const backgroundImageContainer = Styles.createViewStyle({
  position: 'absolute',
  top: 0,
  right: 0,
  bottom: 0,
  left: 0
})

export const imageBackgroundPreview = Styles.createImageStyle({
  flex: 1
})